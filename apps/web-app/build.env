# Siri Environment Variables (Local Dev)

# API Configuration
# See vite.config.ts for proxy configuration
VITE_API_BASE_URL=https://entu-siri.intra.recoverysky.net

# Chat API endpoints (proxied through Vite)
# Anonymous (no auth required) - used when user is not logged in
VITE_ANON_CHAT_API_URL=http://entu-siri.intra.recoverysky.net/api/v1/chat
# Authenticated - used when user is logged in (includes user context)
VITE_AUTH_CHAT_API_URL=https://entu-siri.intra.recoverysky.net/api/v1/chat

# Transcription API (for VAD voice input - @siri/web-api)
VITE_TRANSCRIPTION_API_URL=https://entu-siri.intra.recoverysky.net

# Zitadel Authentication (disabled for local dev)
VITE_DISABLE_AUTH=true
VITE_ZITADEL_AUTHORITY=https://auth.rso
VITE_ZITADEL_PROJECT_ID=352079615960809474
VITE_ZITADEL_CLIENT_ID=352079658088464386

# use for local dev testing with vite
# VITE_ZITADEL_REDIRECT_URI=https://entu-siri.intra.recoverysky.net:5173/callback
# live production redirect
VITE_ZITADEL_REDIRECT_URI=https://entu-siri.intra.recoverysky.net/callback

# Feature Flags (optional)
VITE_ENABLE_VOICE_INPUT=true
VITE_ENABLE_TYPEWRITER=true
VITE_ENABLE_VAD_VOICE=true        # Use VAD auto-detection instead of hold-to-talk
