# RecoverySky App Environment Variables

# API Configuration
VITE_API_BASE_URL=http://localhost:4000

# Chat API endpoint (authenticated users only)
VITE_AUTH_CHAT_API_URL=http://localhost:3333/api/v1/chat

# Transcription API (for VAD voice input - @pippa/api)
VITE_TRANSCRIPTION_API_URL=http://localhost:3001

# Zitadel Authentication (REQUIRED)
VITE_ZITADEL_AUTHORITY=https://auth.example.com
VITE_ZITADEL_CLIENT_ID=<your-client-id>
VITE_ZITADEL_REDIRECT_URI=http://localhost:5173/callback
# Project ID required for JWT access tokens (find in Zitadel console)
VITE_ZITADEL_PROJECT_ID=<your-project-id>

# Feature Flags (optional)
# VITE_ENABLE_VOICE_INPUT=true      # Enable voice input (hold-to-talk or VAD)
# VITE_ENABLE_TYPEWRITER=true       # Enable typing animation for AI responses
# VITE_ENABLE_VAD_VOICE=true        # Use VAD auto-detection instead of hold-to-talk
